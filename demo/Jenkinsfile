pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
                gradle build
            }
        }
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
        }
    }
}
