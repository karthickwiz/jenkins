pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'               
            }
        }
    }
    post { 
        always { 
             deleteDir()
            echo 'I will always say Hello again!'
        }
    }
}
