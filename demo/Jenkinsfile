pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
                git clean -fdx
               // bat 'set'
                echo 'done'
            }
        }
    }
    post { 
        always { 
             deleteDir()
            echo 'I will always say Hello again!'
        }
    }
}
