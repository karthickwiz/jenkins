pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
                cd demo/
                bat 'gradlew.bat'
            }
        }
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
        }
    }
}
