pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'              
            }
        }
        
        stage ('Clone') {
             steps {
        git url: 'https://github.com/karthickwiz/jenkins.git'
             }
    }
         stage ('build') {
             steps {
        git clone https://github.com/karthickwiz/jenkins.git
             }
    }
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
        }
    }
}
