pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'   
                 bat 'set'
            }
        }
    }
    post { 
        always { 
             echo 'I will always say Hello again!'
        }
    }
}
